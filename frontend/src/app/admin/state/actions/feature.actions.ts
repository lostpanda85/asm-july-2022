import { createAction, props } from '@ngrx/store';

export const FeatureEvents = {
  FeatureEntered: createAction('[admin] feature entered'),
};

export const FeatureCommands = {};

export const FeatureDocuments = {};
